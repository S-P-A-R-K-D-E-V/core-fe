import { NotificationConfigView } from 'src/sections/notification-config/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Cấu hình thông báo',
};

export default function NotificationConfigPage() {
  return <NotificationConfigView />;
}
