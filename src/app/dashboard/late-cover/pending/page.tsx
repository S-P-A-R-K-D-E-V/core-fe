import PendingLateCoverRequestsView from 'src/sections/late-cover/view/pending-late-cover-requests-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Duyệt yêu cầu làm hộ',
};

export default function PendingLateCoverRequestsPage() {
  return <PendingLateCoverRequestsView />;
}
