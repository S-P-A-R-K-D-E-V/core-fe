import SalaryHistoryView from 'src/sections/salary-history/view/salary-history-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Lịch sử thay đổi lương',
};

export default function SalaryHistoryPage() {
  return <SalaryHistoryView />;
}
