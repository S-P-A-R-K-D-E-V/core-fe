import { CategoryCreateView } from 'src/sections/pos/category/view';

export const metadata = { title: 'Tạo danh mục mới' };

export default function CategoryCreatePage() {
  return <CategoryCreateView />;
}
