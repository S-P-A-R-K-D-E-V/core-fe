import { SettlementPreviewView } from 'src/sections/shareholder/view';

export const metadata = { title: 'Đối chiếu & Chốt sổ' };

export default function SettlementPreviewPage() {
  return <SettlementPreviewView />;
}
