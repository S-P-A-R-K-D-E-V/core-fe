import { ProductCreateView } from 'src/sections/pos/product/view';

export const metadata = { title: 'Tạo sản phẩm mới' };

export default function ProductCreatePage() {
  return <ProductCreateView />;
}
