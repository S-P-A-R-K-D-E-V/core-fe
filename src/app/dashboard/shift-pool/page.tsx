import { ShiftPoolUnifiedView } from 'src/sections/shift-pool/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Đổi ca & Làm hộ',
};

export default function ShiftPoolPage() {
  return <ShiftPoolUnifiedView />;
}
