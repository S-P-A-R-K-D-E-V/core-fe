import { PendingPoolView } from 'src/sections/shift-pool/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Duyệt đổi ca & làm hộ',
};

export default function PendingPoolPage() {
  return <PendingPoolView />;
}
