import { ShiftTemplateCreateView } from 'src/sections/shift/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create Shift Template',
};

export default function ShiftTemplateCreatePage() {
  return <ShiftTemplateCreateView />;
}
