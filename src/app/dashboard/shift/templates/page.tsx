import { ShiftTemplateListView } from 'src/sections/shift/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Shift Templates',
};

export default function ShiftTemplateListPage() {
  return <ShiftTemplateListView />;
}
