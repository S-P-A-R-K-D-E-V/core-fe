import { MessengerView } from 'src/sections/messenger/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Tin nhắn nội bộ',
};

export default function MessengerPage() {
  return <MessengerView />;
}
