import { ShiftRegistrationView } from 'src/sections/attendance/view';

export const metadata = {
  title: 'Dashboard: Đăng ký ca làm việc',
};

export default function ShiftRegistrationPage() {
  return <ShiftRegistrationView />;
}
