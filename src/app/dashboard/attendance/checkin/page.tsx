import { AttendanceCheckinView } from 'src/sections/attendance/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Check In/Out',
};

export default function AttendanceCheckinPage() {
  return <AttendanceCheckinView />;
}
