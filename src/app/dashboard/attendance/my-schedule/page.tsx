import { MyScheduleView } from 'src/sections/attendance/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: My Schedule',
};

export default function MySchedulePage() {
  return <MyScheduleView />;
}
