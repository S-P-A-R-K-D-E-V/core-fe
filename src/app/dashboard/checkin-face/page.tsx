import { CheckinFaceView } from 'src/sections/checkin-face/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Check-in Khuôn mặt',
};

export default function CheckinFacePage() {
  return <CheckinFaceView />;
}
