import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Nội bộ test :)',
};

export default function HomePage() {
  return <HomeView />;
}
