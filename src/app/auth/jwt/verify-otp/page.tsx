import { JwtVerifyOtpView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = { title: 'Jwt: Verify OTP' };

export default function VerifyOtpPage() {
  return <JwtVerifyOtpView />;
}
